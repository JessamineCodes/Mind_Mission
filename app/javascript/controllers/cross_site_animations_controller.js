import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="cross-site-animations"
export default class extends Controller {
  connect() {
    "Cross site controller connected"
  }
}
